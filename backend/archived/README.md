# Archived Scrapers

This directory contains old/unused scraper versions for reference.

## Files

- **`scraper-dev.js`** - Early development version with different output format. Replaced by scraper-prod.js.
- **`test-scraper.js`** - Network connectivity test script used for debugging.

## Current Scraper

The active scraper is `../scraper-prod.js`, used for both development and production.

Run it with:
```bash
npm run scrape
```
